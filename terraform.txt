terraform init, plan, apply -auto-approve, destroy, validate, fmt

resource block >> arguments

output block  >> attributes

data block

variables:

command line
terraform.tfvars
env   >> export TF_VAR_instance_type="t3.large"
variables.tf


loops:

count > index base > works with list
for_each  > works with map and set
dynamic block


permissions .ssh folder 700
authorized_keys 600